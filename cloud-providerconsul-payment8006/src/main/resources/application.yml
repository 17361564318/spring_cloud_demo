server:
  port: 8006
spring:
  application:
    name: consul-provider-payment
    #consul注册中心
  cloud:
    consul:
      discovery:
        service-name: ${spring.application.name}
      host: localhost
      port: 8500